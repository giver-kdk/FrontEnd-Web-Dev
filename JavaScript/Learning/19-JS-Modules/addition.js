// Export Default means the exporting content is the default thing to be exported by the module
// Only one default export can be done through a sinngle module

// Inline Default Export
export default function add(a, b)
{
	console.log("Sum of two numbers: " + (a + b));
}