// substring() is similar to slice() but it doesn't accept negative parameters
let myName = "Giver Khadka";
let firstName = myName.slice(0, 5);
// let firstName = myName.slice(0, -2);			// Not Allowed
console.log(myName);
console.log(firstName);
