// indexOf() return the index of 1st occurrance of a sub-string in a string
// Actually, base index(index of 1st character) is returneed
// Returns 1- on failure
// Searches from stat-to-end
let myName = "My name is Giver Khadka";
let index1 = myName.indexOf("Giver", 3);			// Searches from index-3 to end
console.log(index1);
