// toUpperCase() returns a string that is converted into uppercase
// It doesn't modify the original string

let myName = "Giver Khadka";
let upperName = myName.toUpperCase();
console.log(myName);
console.log(upperName);