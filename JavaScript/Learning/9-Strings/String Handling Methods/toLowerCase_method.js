// toLowerCase() returns a string that is converted into lowercase
// It doesn't modify the original string

let myName = "Giver Khadka";
let lowerName = myName.toLowerCase();
console.log(myName);
console.log(lowerName);