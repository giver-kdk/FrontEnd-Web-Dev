// trim() returns a new string by removing whitespaces from both sides of a string
// It doesn't remove whitespace in between the characters of string
// It doesn't modify the original string

let myName = "         Giver    Khadka     ";
let pureName = myName.trim();
console.log(myName);
console.log(pureName);