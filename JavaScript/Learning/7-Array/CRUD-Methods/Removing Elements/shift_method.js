// shift() removes the first array element
// It can modify the actual array
// It shifts other array elements towards starting
// It returns the removed element

let group = ["Ram", "Shyam", "Hari"];
let removedItem = group.shift();

console.log(removedItem);
console.log(group);

