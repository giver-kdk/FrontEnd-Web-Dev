// pop() removes the last array element
// It modifies the original array
// It return the removed element 

let group = ["Ram", "Shyam", "Hari"];
let removedItem = group.pop();


console.log(removedItem);
console.log(group);
