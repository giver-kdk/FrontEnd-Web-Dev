// push() adds item from end of array
// It actually modifies the original array and its length
// It return the updated array length

let group = ["Ram", "Shyam", "Hari", "Sita"];

let newLength = group.push("Gita", "Rita");
console.log(newLength);
console.log(group);